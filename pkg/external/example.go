package external

// UsersCanImport is an example function to show
// that package within /pkg folder can be imported
// by end users who need exported functions from
// the package(s)
func UsersCanImport() bool {
	return true
}
